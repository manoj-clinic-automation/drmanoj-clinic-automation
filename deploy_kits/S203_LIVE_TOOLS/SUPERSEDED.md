# SUPERSEDED — DO NOT RESTORE FROM THIS KIT

**Superseded by `S205_LIVE_TOOLS` (and, for the files that have since drifted,
by `S212_LIVE_TOOLS`). Marked at S212, 31-Aug-2026. Nothing deleted.**

Measured today against the running machines: **6 of its 15 files are stale** —
4 of 10 on manojz, 2 of 5 on the medical PC. Restoring from it would put back
pre-fix bytes, including the `PULL_FROM_MEDICAL.bat` that predates F-196.

**Its own gate passes 15 of 15.** That is exactly the trap: `SUMS.md5` hashes
the kit against itself, so a kit can be perfectly intact and completely wrong
about the machine. That is F-215, and this folder is its clearest example.

Kept for the record. Never for a rebuild.

S226_STOCK_CLOSE -- ONE PASTE on the VPS after the publish. ONE LINE.
Three files replaced: stock_app.py, padwriter.py, stock_check_live.html.
Refuses -- and changes NOTHING -- unless the kit sums check and the live pins are
those S226_PAD_ONPAGE printed: stock_app.py 4578b725, stock_check_live.html ed47601b.

git -C /root/deploy/repo fetch --depth 1 origin main && git -C /root/deploy/repo reset --hard origin/main && cd /root/deploy/repo/deploy_kits/S226_STOCK_CLOSE && md5sum -c SUMS.md5 --quiet && [ "$(md5sum /root/finance/stock_app.py | cut -c1-8)" = "4578b725" ] && [ "$(md5sum /root/finance/stock_check_live.html | cut -c1-8)" = "ed47601b" ] || { echo "REFUSED: kit sums or the live pins do not match -- NOTHING CHANGED"; false; } && for f in stock_app.py padwriter.py stock_check_live.html; do \cp /root/finance/$f /root/finance/$f.bak_S226close_$(date +%Y%m%d); done && \cp stock_app.py padwriter.py stock_check_live.html /root/finance/ && (/root/wa/venv/bin/python3 -B -c "import py_compile;[py_compile.compile('/root/finance/'+f,doraise=True,cfile='/tmp/s226c_'+f+'.pyc') for f in ('stock_app.py','padwriter.py')]" && systemctl restart clinic-finance.service && sleep 3 && systemctl is-active --quiet clinic-finance.service && echo "UP -- steps 1-2-3-4, named sheets, and Close the stock check are live" && md5sum /root/finance/stock_app.py /root/finance/padwriter.py /root/finance/stock_check_live.html || (echo "COMPILE FAILED OR SERVICE DID NOT COME BACK -- ROLLING BACK BY ITSELF"; for f in stock_app.py padwriter.py stock_check_live.html; do \cp /root/finance/$f.bak_S226close_$(date +%Y%m%d) /root/finance/$f; done; systemctl restart clinic-finance.service; sleep 3; systemctl is-active clinic-finance.service; journalctl -u clinic-finance.service -n 12 --no-pager)); rm -f /tmp/s226c_*.pyc

Expected at the end:  UP -- steps 1-2-3-4, named sheets, and Close the stock check are live
                      fdcdf873dc217284ad78563195f52a71  /root/finance/stock_app.py
                      77c43e470d5ffd88019c4469281690b1  /root/finance/padwriter.py
                      6fd3b7f96e24509a6124854946cd12b7  /root/finance/stock_check_live.html

THE PAGE, for Amir:   https://followup.dr-manoj.in/finance/stock/page/count
  1  the four details   2  Download SHEET 1   3  Upload the filled sheet
  4  the box says what was recorded and what is left ("Some stock count is left ... download the
     sheet and complete it" -> SHEET 2 REMAINING, 3, 4 ...). When the server says everything is
     counted, "Close the stock check" appears -> STOCK CHECK COMPLETED -> FINAL RESULT sheet; that
     count takes no more sheets (a late sheet is refused in words). A fresh SHEET 1 starts the next count.
  The doctor (checker) alone sees "Doctor: close count #N as it stands" on an open count with work left --
  in the box and under Recent stock checks -- so an abandoned count can be closed and the analysis run.

ROLLING BACK = the three .bak_S226close_* files beside the live ones, \cp each back, restart.

OFFLINE EVIDENCE -- 169 checks in a real browser at phone width (390px):
  EVIDENCE_pad_onpage_s226.txt              92/92 the loop, the close, the refusal after close, the doctor's
                                                  close-as-it-stands, a counter refused by the server, names of sheets
  EVIDENCE_regress_count_search_s226.txt    34/34 the on-screen count, unchanged
  EVIDENCE_regress_submit_flow_s226.txt     30/30 report and send, unchanged
  EVIDENCE_regress_safestore_s226.txt       13/13 the safe store, unchanged
  The FINAL RESULT workbook opens in LibreOffice without error.

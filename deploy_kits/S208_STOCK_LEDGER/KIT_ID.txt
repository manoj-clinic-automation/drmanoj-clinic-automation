KIT      S208_STOCK_LEDGER
BUILT    28-Aug-2026 IST, attended session on manojz
PURPOSE  the stock loop on the clinic server:
         expected (Marg) -> counted (staff) -> difference -> cause -> closed
SUPERSED S207_STOCK_VPS. Do NOT install that kit. Its push could never have
         reached the server: the MARG token was sent in the CRON header, to a
         path the Marg token was not allowed to open. Differences would have
         been raised correctly and never closed.
STATE    STAGED, NOT INSTALLED. The VPS was not touched and no credential for
         it exists in the session that built this, by design.
FILES    stock_app.py           the blueprint. S208: the snapshot route now
                                accepts the sender's token OR a signed-in
                                checker/maker; /api/open and /api/losses are
                                role-checked in this file, not only at the gate
         stock_schema.sql       five tables, added to the EXISTING finance.db
         patch_finance_app.py   the two edits, by program, idempotent,
                                byte-exactly reversible, refuses on any doubt
         selftest_stock_app.py  44 checks against the real routes, temp db
         selftest_gate_join.py  14 checks: real header -> real gate -> real
                                route. THE PROOF S207 DID NOT HAVE
         push_snapshot.py       runs on MANOJZ. S208: sends X-Finance-Marg,
                                and --verify proves the join without writing
         install_stock_ledger.sh  the gated installer
PROVEN   44/44 · 14/14 · patch applied to the real 11,395-line finance_app.py:
         compiles, and revert is md5-identical (70f79997...) · the patcher
         REFUSES on the older finance/finance_app.py copy, as it should
INSTALL  bash /root/deploy/vps_deploy.sh S208_STOCK_LEDGER

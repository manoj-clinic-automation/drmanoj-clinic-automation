# KIT_MANIFEST.txt -- NOT USED BY THIS KIT. Kept only so an older copy of this
# folder is not mistaken for a live Drive delivery.
#
# S259's medical half does NOT go down the Google Drive kit channel. It is sent
# from manojz over the Tailscale share the Marg pull already uses, by
#     INSTALL_S259_MEDICAL.bat  in this folder.
# The reason is written up in the kit README and in F-473: the channel itself is
# sound, but the assistant could not carry 17 KB of base64 through the Drive
# connector without a one-byte slip -- twice, both caught by the file size.
#
# Nothing below this line is read by anything.

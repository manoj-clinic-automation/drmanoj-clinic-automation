# S339_RECORDS_TILE

The owner's two small items (20-Sep-2026): a **Patient records** tile on the portal (section Clinic, doctors by role; the server's own unit refuses everyone else), and the read-only Drive proof that S335's installer printed wrongly (a library warning was read as data). Nothing else changes.

```
cd /root/deploy/repo && git pull --ff-only && bash /root/deploy/repo/deploy_kits/S339_RECORDS_TILE/install_S339_RECORDS_TILE.sh
```

Pin: `/root/portal/portal.py` `4085b767…` → `b7b0e43ccb4d6b012b38b9016f3f6f0c`. tile_grants.json untouched (v22).

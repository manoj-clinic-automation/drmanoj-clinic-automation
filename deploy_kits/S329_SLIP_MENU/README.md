# S329_SLIP_MENU

The slip tile opens a menu (one screen per job, Back to menu); the separate upload tile is withdrawn (grants v22, portal back to S324 bytes). One upload line and one tap per X-ray. The doctors' report lists only pending uploads; Shavez has the report in his menu. Walk: 109 checks.

```
cd /root/deploy/repo && git pull --ff-only && bash /root/deploy/repo/deploy_kits/S329_SLIP_MENU/install_S329_SLIP_MENU.sh
```

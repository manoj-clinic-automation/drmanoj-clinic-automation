# S216_ORDERS — live pin record
*Recorded at install, 01-Sep-2026 (F-97: as the pin moves).*

| file on the box | before | after | == kit bytes |
|---|---|---|---|
| `/root/portal/casepack_portal.py` | `3146bdbf…` | **`760e8c36…`** | **yes** |
| `/root/wa/casepack/casepack_page.html` | `63b2cba4…` | **`d5d4a3e7…`** | **yes** |

**The portal pin moved for the first time since S215.** Both backups named and
kept on the box (`*.bak_S216_ORDERS_*`).

Owner-run on the box: selftest **32/32** AND **ORDERS_SERVER_TEST 19/19** —
the medicine list proven against the live server, not only offline. That
included the seed, persistence of his marks, the dated backup, and all three
refusals (empty list · blank names · absurd list) with his list intact after.

New file created on the box: `/root/wa/casepack/med_list.csv`, seeded once from
`MY_TEMPLATES_S216.txt`. Never re-seeded after that; it is his.

**S216 CLOSING POSITION — one page-pin chain, one portal move:**
page `903b915e…` (S215) → guard → contrast → names → back-nav → smart → tone →
**`d5d4a3e7…`**; portal `3146bdbf…` → **`760e8c36…`**.
Only the last of each is current. The Register moves each row ONCE.

**LIVE-SHAPE WALK: OWED** — enter a surgery time, build all three sheets, and
open “Edit list” to mark one medicine Ayushman and save it.

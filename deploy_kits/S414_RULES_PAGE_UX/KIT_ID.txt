kit id S414_RULES_PAGE_UX
project PARENT (clinic), fault F-635, follow-up to S410 (session 283, 26-Sep-2026) -- the buying-rules block on the owner's approvals page rewritten: nothing redraws on a tap (ticks, supplier edits, pause, freeze, approve patch the DOM in place from the API's answer; a full draw keeps every open list and the scroll), the two candidate lists are check-lists that stay put (count, tick / untick all shown, filter, sort, ticked-only; a ticked row stays greyed with its rule), the per-supplier editor is an inline form with a hint on every field, Freeze / Approve are inline confirmations (no prompt, no confirm dialog), 'Your item rules' grouped by rule with a cross and inline numbers and an autocomplete add box, Approve rules with its one-line summary at the top and the bottom; order_rules.py gains GET /finance/porders/api/rules/items?q= and the item-rule answer carries the counts and the row; finance_approvals.html + order_rules.py patched from their live bytes; restarts clinic-finance

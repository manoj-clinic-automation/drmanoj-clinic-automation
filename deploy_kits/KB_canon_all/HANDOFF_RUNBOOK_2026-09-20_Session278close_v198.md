# HANDOFF RUNBOOK — v198 — S278 CLOSE (the parent), 20-Sep-2026

*Supersedes v197 (S277 close, the parent). The Sanjeevni thread's own record is v194 (S274 close).*

## §0 · WHAT HAPPENED
Phase 0 green (canon 690/690; the nightly REBUILT · WARN · FIXED; pins 127 / 8 / 249, the eight installed after the 01:35 bundle). The X-ray test folder still held one evening of staff files, so the live filing waits. On the owner's delegation (*"a pure tech call … you are the boss"*) the unattended lane got its second store: the scheduled task now reads `START_HERE_PROMPT_v10`, and its nightly report plus a dated queue snapshot go into Drive `Clinic_Unattended_Reports`, which the 03:10 nightly's new step 0 (`unattended_pull.py`, kit **S354**, manojz) copies into the Cowork tree before the manifest rebuild (D588). The owner found his Vitals & Plan tile missing: it is a clinic-PC-only tile and the tool was never moved to the VPS (F-598); the move is on the list, last. He then re-ordered the work (D589): the Callback Tracker's sign-in once, then the incoming caller shown on the staff phone, then the rest of Block C, then the mails, the tiles, the two follow-ups and Vitals. The caller pop-up was first proposed over ntfy; he corrected it (free tier, high volume) and had the plan re-read — S277 had misstated the record (F-597) — and ruled the MyOperator APIs ready to build. D590 is the design, read from the vendor's own webhook documents. No VPS code this session.

## §1 · MENTAL MODELS
- **A paraphrase of a decision is not the decision** (F-597). "Read it back from the record" is only safe if the brief that says it has itself read the record.
- **A hidden tile reads as a lost one** (F-598). A migration wave that stopped part-way must say so where the owner looks.
- **ntfy is for rare alerts, not traffic.** The free tier carries few messages; a per-call channel is Web Push through the Clinic app, which has no per-message quota.
- **A second webhook entry, never a change to the live receiver.** MyOperator's Webhooks v2 takes several entries; a new event set gets its own path and its own service.
- **The unattended lane cannot publish and cannot reach the PC; Drive is the bridge.** A second store for a cloud-only writer is a folder the nightly pulls, copy-only.

## §2 · THE LIVE BACKLOG
`S278_BUILD_BRIEF.md §2` and `START_HERE_SESSION_279.md §0` carry the order (D589). First proofs due on 21-Sep: `D:\Downloads\_kbtools\UNATTENDED_PULL_LATEST.txt` after 03:10 (the probe file + the 21-Sep report + a queue snapshot copied), the 01:35 bundle carrying `root/finance/spine/` and the eight post-bundle pins, the 11:39 Daily Clinic Report's real attendance. The live X-ray filing still goes first the day the test folder holds 2–3 days of staff files. Carried: the phonebook from the two exports · the last four units into the bundle (his ruling) · fault injection for `backup`/`outbox` · Bhati's petty-book layout (offered) · Docterz uploads stop on his word · bank-SMS PARKED · sending reports to patients on WhatsApp (F-82).

## §3 · INSTALL DISCIPLINE — added this session
- A change to a manojz nightly tool: back up the file beside itself with its md5 in the name, edit with an anchor count asserted, keep CRLF, add the kit's own `S###_SUMS.md5` and a copy under `ClaudeCowork\02_SESSION_KITS\S###\`, and re-pin it in the Register the same close.
- A selftest is shown able to fail by a one-line mutation before it is called green.
- A scheduled task's prompt is updated in place (`update_trigger`), never deleted and recreated — the run history stays.

## §4 · THE BOUNDARY
The VPS needs the owner (one line each). Apps Script needs only his sign-in (drmka.ortho, `/u/1/`); every placement updates `GAS_CURRENT` (D583). manojz: `device_bash` works again; the file tools reach all three roots. MyOperator's panel: the second webhook entry is a settings change — made by the assistant in his signed-in browser if the session can reach it, otherwise his one GUI step. The Sanjeevni chat (S275 reserved) writes the same canon folder: list `KB_canon_all\` before writing (F-534). HOLD 2 stands for the Callback Tracker except its email triggers (D586) and the D589/D590 work he has ordered. Next free after this close: **D591 · F-599 · A-D25 · kit S355 · Session 279** (mirror; `board/_numbers` is the source; 279 is the parent's next chat).

# END OF SESSION — full session close-out (v11)

**Say "EOS" (build session) or "EOS-light" (no live code touched).** The routine below runs from
project knowledge. Re-paste only to change the routine.

**v11 in one line (S208, owner directives):** three additions earned in the seven-kit night —
**A14** (PC session-file cleanup: a close that leaves scratch scattered on the owner's disk is
not finished), **A15** (the assistant EXECUTES the publish via computer control and verifies
from the bat output — device_bash must never run git against the repo, F-233), and the
session-START rule moved into the evergreen prompt: **connections first** — folders AND the
assistant's signed-in browser are checked before any work, because S208 spent its only blocked
hour waiting on a sign-in nobody had asked for.

**Lineage:** v10 S207 (path fix + SSD + A13 tranche) · v9 S206 (A12) · v7 S201 (A10) · v6 S194
(A9) · v5 S188 (A8) · v4 D247 (Register+Archive+manifest).

---

## A0. THE EVIDENCE RULE — unchanged from v10, read it there in full
Re-keyed text corroborates, never convicts · a filename is not provenance · a path in a document
is not provenance · the newer store is not automatically right · ask every green check what
question it answers · an empty or small result is not good news · **(added v11) a green
selftest of a kit proves the kit, not the join — walk the LIVE shape** (S208: two defects sat
behind 65 green checks).

## A. DOCUMENT UPDATES — A0–A13 exactly as v10 (Archive append w/ prefix-hash proof · Register
current-state + the four self-referential lines · Runbook · START_HERE · Tier-1 only-if-changed
· Tier-2 never · manifest after all · live pins + KB_canon_all refresh · Notion page ·
OWNER_TODO_LIVE · live-PC-tools capture vs LIVE source · the Cowork folder + SSD mirror &
retention & build brief · ONE reduction tranche, measured not projected). v10's full text
remains the reference for A0–A13; v11 adds:

**A14. CLEAN THE OWNER'S PC OF SESSION SCRATCH (owner directive, S208).** Enumerate everything
this session created outside its proper stores — `__pycache__` swept to `_to_delete_*`, browser
download temps, stray CSVs/logs in `Downloads` or `margsync` — consolidate under ONE
`_to_delete_S###\` per drive-root with a `WHY_SAFE.txt` naming each file's surviving copy (path
+ md5), and list it in the close report for the owner's one-click delete. **Moving is the
agent's; deleting is the owner's; and nothing moves without its safe-copy named.**

**A15. THE PUBLISH IS THE ASSISTANT'S TO EXECUTE (owner directive, S208).** At close (and at any
mid-session publish the owner approves): run `D:\dr-manoj-git\drmanoj-clinic-automation\
PUBLISH_ALL_.bat` **via computer control** (never via device_bash — F-233: the sandbox strands
`.git\index.lock`), read the window's output, and **verify the success line (origin HEAD ==
local HEAD) before reporting published**. The phone gate is inside the bat; if the bat refuses,
the refusal text is the finding. Only VPS-side actions remain the owner's.

## B–F. As v10 (Notion/Drive/Gmail · GitHub commit text · knowledge swaps · cold kit cadence ·
maintenance check), with the cold-kit table updated each close.

| | |
|---|---|
| **last cold kit** | **S208, 30-Aug-2026** — see close report for name + md5 |
| **next due** | **S211, or the next Register/Archive bump — whichever is first** |

---
*v11 changes from v10 (S208): A14 PC cleanup · A15 assistant-executed, output-verified publish ·
start-of-session "connections first" (folders + signed-in browser) promoted into the evergreen
START_HERE · A0 gains the live-shape-walk corollary.*

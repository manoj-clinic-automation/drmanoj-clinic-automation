# HANDOFF RUNBOOK — v197 — S277 CLOSE (the parent), 20-Sep-2026

*Supersedes v196 (S276 close, the parent). The Sanjeevni thread's own record is v194 (S274 close).*

## §0 · WHAT HAPPENED
The X-ray test folder held nine staff files over one and a half days — not the 2–3 days the live filing waits for — but the page read **0 of 9 matched**, because the staff write the clinic ID at the END of the name (`NAME 1234.jpg`) and the plan read it only at the start. Three kits, all live from the owner's lines the same afternoon: `S351_XRAY_NAMES` (the ID anywhere, the sister rule, two numbers → a person; 6 of 9), `S352_GAS_REPO_COPY` (F-591 closed: the Sunday drift check's repository copy is now the living `deploy_kits/GAS_CURRENT/`, one conf line on the box; the old photograph was behind on UPIReconciliation too, F-595) and `S353_XRAY_FILMS` (the owner's ruling D584 — one film per study, Knee studies two — after one question; 8 of 9). Forty-one checksum backups moved out of canon (F-596). Then, without code, the owner settled the next three blocks of work in his own words (D586, D587): the redundant mails retired after measurement — including the Callback Tracker's own digests, the hold lifted for those triggers only — the Callback Tracker kept and de-frictioned, the PWA tiles re-laid last.

## §1 · MENTAL MODELS
- **A name-shape rule is measured on the staff's own files, never on the brief's example** (F-593). The first real day of files is the specification.
- **When a rule counts physical things, ask the person who makes them** (F-594) — one question, one ruling, one line of code.
- **A check whose only output is a log has no reader** (F-595). Every comparison the estate runs owes its answer to a surface someone opens.
- **A repository copy that must stay current is a living folder with a same-breath rule** (D583), not a frozen kit.
- **Canon holds canon** (F-596): a tool's backups do not live in the folder the gate proves.
- **Every walk keeps one check on the real thing** (F-590) — this session's was the page read live after each install, and it caught F-594.

## §2 · THE LIVE BACKLOG
`S277_BUILD_BRIEF.md §2` and `START_HERE_SESSION_278.md §0` carry the three blocks in the owner's order. The live X-ray filing (`S273_BUILD_BRIEF §2`, D585 inherited) is built the day the test folder holds 2–3 days — 21-Sep and 22-Sep should give them. Tomorrow's evidence: the 21-Sep 01:35 bundle carries `root/finance/spine/` (S347), the 01:50 backup log says ~251 files, the 11:39 Daily Clinic Report shows real attendance (S348), the first `GAS_CURRENT` Sunday read is 27-Sep. Carried unchanged: the phonebook from the two exports · the last four units into the bundle (his ruling) · fault injection for `backup`/`outbox` · Bhati's petty-book layout (offered) · Docterz uploads stop on his word · bank-SMS PARKED · the unattended task's prompt v8 → v10 and proposal (c), both his one line.

## §3 · INSTALL DISCIPLINE — added this session
- A records kit's negative control is the **live** file on the box (`walk_s35x.py <app> /root/finance/records.py`): it must read the new shape as unmatched, or the walk proves nothing about the change.
- A conf change is a kit like any other: pin the script it feeds (`gas_export.py` `072a9411`), back up the conf with mode 600, write exactly one line, never print the file, and end with the job's own read-only mode (`diff`) so the paste shows the result.
- A living folder in `deploy_kits/` carries its own `SUMS.md5`, a `READ_ME` naming the source of every file, and exact-path `.gitignore` allow lines for any blanket-ignored type — proven with real git in a cloud clone before the publish is named.
- The page is read live by the assistant after every install that changes what it shows; the count before and after is the proof.

## §4 · THE BOUNDARY
The VPS needs the owner (one line each; three today). Apps Script needs only his sign-in (drmka.ortho, `/u/1/`) and every placement now also updates `GAS_CURRENT`. manojz: `device_bash` works; the file tools read and write all three roots. The Sanjeevni chat (S275 reserved) writes the same canon folder: list `KB_canon_all\` before writing (F-534). `records.py` is `07ec9b41` and `finance_app.py` `29819879` — a kit touching either takes those as FROM. HOLD 2 stands for everything in the Callback Tracker except its email triggers (D586). Next free after this close: **D588 · F-597 · A-D25 · kit S354 · Session 279** (mirror; `board/_numbers` is the source; 278 is the parent's next chat).

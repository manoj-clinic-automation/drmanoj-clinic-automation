# HANDOFF RUNBOOK — v113 (2026-08-15 · Session 179 close — clinic-finance (medical) LIVE)

*Tier 0. §0 what happened last · §2 live backlog (⭐ top task at head) · §3 install discipline. Companion to the KB Register v5.1 (state) + Archive v1.27 (history). S179 migrated the Sanjeevni (medical) daily-revenue system off Google Forms onto the VPS — a new `clinic-finance` app, live and bank-reconciled. S178 (the KB compaction, EOS-light) is the prior session — see Archive §S178.*

## §0 — WHAT HAPPENED LAST (S179 — FULL; one new live VPS subsystem: clinic-finance)

Owner directive: migrate the daily-revenue system off Google Forms + the Sheet onto the VPS, **medical (Sanjeevni) first**, then clinic, then pathology — *"ALL THREE WILL MIGRATE TO VPS."* Manual workflow stays fallback until each unit runs clean in parallel. Phase 0 green.

- **LIVE:** `clinic-finance` app at `followup.dr-manoj.in/finance/` — system `python3`, gunicorn `127.0.0.1:8106`, OpenLiteSpeed `/finance` context on the portal origin (SSO cookie carries — F-68). Portal tiles **Daily Sale** (maker `darpan`) + **Sanjeevni Medicos** (checker `manoj`). Nightly verified backups (01:05, 30 daily + 12 monthly). `finance.db` holds **121 legacy medical days imported as-recorded** (Apr 1 → Aug 13 2026), reproducing the sheet's own closing to the rupee (−₹30,056).
- **Blocks (all installed with the S177 `.new` + &&-chained md5-gate → `.bak` → `mv` → smoke-gate → restart-or-auto-rollback discipline; smoke proven byte-safe on `finance.db`):** B1 legacy import (36 carry-forward breaks → open dated rows, net −₹84,533; 7 negative-cash days; 14 missing days) · B2 the module (computed opening, non-cash bills, cash movements, expenses w/ salary-advance maker-checker, month close, parked cash, `/api/cutover`) · B3a per-document scanner host pages · B4 day browse (collapsed expandable lines + inline scan links) · **B5 UPI reconciliation LIVE** (`finance_upi.py` parses ICICI MPR `.xlsx`, self-checks vs its own Grand Total, bank is the arbiter; `VPS_Push_UPI.gs` in clinic Gmail pushes daily at 09:30, verified `{"ok":true,"pushed":8}`; MIDs `…312505` Sanjeevni · `…306941` clinic · `…319164` NK Pathology).
- **D313 minted** — the finance subsystem architecture (INTEGER PAISE; computed cash via views; revenue≠cash; deposit never split; missing days shout; patient-spine reads-never-posts; adapter-by-column-map; bank-arbitrated UPI; fail-closed auth; clinic+lab a replication). **F-84 minted + FIXED** — three self-found security faults (ungated reads · spoofable header identity · unchecked epoch), all "the offline-testing shortcut was the vulnerability"; installer auto-rolls-back on `sso_epoch_ok:false`.
- **Marg feed analysed, adapter not yet built:** the Sanjeevni Marg sale-report `.xls` beats Sarvam (13/13 exact day matches). Rule for the adapter: cash = the CASH column, UPI = net − CASH (never the mode field). The description trailing number behaves like the clinic patient ID (owner to confirm). Its own adapter needed (date in group-header rows; BIFF `.xls`; day-total self-check). Marg `.jmbkh` backup = encrypted dead end; keep exporting the report. See `S179_Marg_Sale_Report_Analysis`, `S180_Marg_Folder_Recon`.
- **Sole live-state reference:** `S179_Finance_LIVE_State` (Tier-1, refreshed to final md5s at this close). Full narrative: Archive §S179. Git kit `gitkit_S179.zip` prepared. **Next free: D314 · F-85 · Session 180.**

## §2 — LIVE BACKLOG

⭐ **S180 top task — CLINIC + LAB finance modules (largely a replication of medical).** Medical is streamlined; clinic and lab now migrate onto the same `clinic-finance` app (per-unit isolation already in the schema). Needs: forensic read of the clinic + lab tabs of the source Sheet + their Google Forms; reception/lab-staff logins (`reception`/`labstaff` makers already seeded, `manoj`+`bhawna` checkers); the clinic decision — **clinic reads day-revenue patient-wise from the follow-up tracker** (procedure entry only enriches the procedure name) vs typed entry; lab mirrors medical (Labmate export as the primary source once one real export is mapped). All three stay separate for accounts.

**Finance loose ends (owner, before/with S180):**
1. **The 30-Jul bank-statement deposit** — was it ₹85,000? Gates whether 31-Jul's close can anchor the August re-entry (`Count the drawer`/`/api/cutover`, safe to run twice).
2. **One test scan** through Daily Sale (end-to-end scanner check).
3. **Off-box backup destination** — copies are same-disk (good vs a bad deploy, useless vs losing the VPS); the file holds patient names, so where it goes is the owner's call.
4. **Accountant-pack patient-name toggle** (`export.include_patient_names`, default off).
5. **Marg-feed decision** — call Marg support for a scheduled `.xls`/Tally export; optionally a Cowork recon on the MEDICAL PC (`D:\MARGERP`, 192.168.1.37) — never open `*licence*`/`*sqlconnection*`/`*password*` files, no patient names/full phones.
6. **Not yet built:** B4b Drive archive mover · B6 salary-advance → Staff-Ledger post · B7 month export `.xlsx`/`.pdf` per entity · missing-day WhatsApp nudge cron · Marg/Labmate column maps · pathology UPI route beyond the merchant id.
7. **Owner action — commit `gitkit_S179.zip`** to `drmanoj-clinic-automation`: `finance/` + `gas/VPS_Push_UPI.gs`. **`.gitignore` the PHI paths (`finance/finance.db`, `finance/scans/`, `finance/exports/`, `finance/medical_*.csv`, `finance/access.log`) in the SAME commit, BEFORE `git add finance/` (F-31/F-49)** — the kit's `.gitignore.additions` + README + COMMIT_MESSAGE.txt spell it out. Token rotation offered (`FINANCE_CRON_TOKEN`).

**Carried from before (still open):**
- ⭐ **WABA GO-LIVE (F-82).** Still BLOCKED vendor-side (MyOperator HTTP 500 on authenticated calls). Fast probe `GET /chat/templates` → 200 not 500; then `PORTAL_WA_DRYRUN → "0"` in `/root/portal/portal_config.py` → restart `clinic-portal.service` → self-send `drmanoj_post_visit` to the doctor's own number → live. No code change. **Coordinate with Lokesh before ANY WABA token rotation.**
- **Owner action — commit the S177 asset git kit** (`gitkit_S177.zip`, lifts assetapp to v1.11.0); behind it the S162–S172 clinic-side mirror backlog (portal/console/casepack — `.gitignore` PHI first).
- **Security:** rotate the Asset Register `/api/due` WhatsApp token + the portal `/api/due` token; service-account key rotation (Tier A, coordinate Lokesh); `wa_approve` nohup→systemd verify-first; unaccounted `ANTHROPIC_API_KEY` in `/root/wa/.env`.
- **Asset rows #45–50 review** (dup vendor entries — keep or delete). Asset A-D25 candidates: durable OCR worker (F-83) · consumption dashboards · Phase-E Sarvam tuning.
- **Console follow-ons:** w8/w9 redesign · D223 gist tile build · nightly `console_reviews.db` → Drive backup · F-81 duplicate call rows · MyOperator OBD click-to-call · outcome-options admin UI. F-69 dead `Call_Feed` writer · F-70 Callback Tracker Core Dossier update · F-71 rotation check.
- **August salary reconciliation** at month-end (first real APPROVE ~Sep 01–09). Docterz export migration (parked).

## §3 — INSTALL DISCIPLINE
Files delivered **pre-named `.new`**; install = **ONE &&-chained block**: `md5sum -c` the `.new` files → `cp live live.bak` → `mv .new → live` → **run the smoke test as a gate** → restart the service ONLY on green → auto-restore from `.bak` on red. Never numbered steps; never pasted heredocs (owner terminal corrupts long pastes — ship scripts as uploads). A filename is not provenance — trust the hash (F-66). **The finance app AND the asset app run on the system `python3`** (gunicorn `#!/usr/bin/python3`), NOT the `/root/wa` venv (F-53); clinic `/root/wa` scripts use `/root/wa/venv/bin/python3`. Portal lives at `/root/portal/`, finance at `/root/finance/`. Flask route change → F-63 test-client hits on ACTUAL routes; served-HTML assertions incl. absence checks (F-79). New/altered table → `--init`/CREATE-IF-NOT-EXISTS before the page queries it (F-65). One writer per store (D235). Never slice a date string — parse it (F-78). **Finance auth is fail-closed (F-84): SSO cookie authoritative, before_request allow-list, per-unit `unit_role` entitlement, live epoch check, header auth opt-in only; the installer gates on `healthz sso_epoch_ok`.** WhatsApp go-live = DRY→live + self-send-to-own-number FIRST (D310). **Salary/PHI/finance.db never in repo/kit — `.gitignore` the finance PHI paths BEFORE the first `git add` (F-31/F-49).** DB mutations ship as guarded scripts (refuse-all identity check + WAL-checkpointed backup + all-or-nothing txn), never ad-hoc SQL.

**END OF HANDOFF RUNBOOK v113 (Session 179).**

# START HERE — SESSION 209 (generated at the S208 close, 30-Aug-2026)

Hi Claude. Continuing the clinic-automation project — **Session 209**.
Owner: Dr. Manoj Agarwal, orthopaedic surgeon, Bareilly. Working protocol: plain language ·
one step at a time · full-file replacements · mask numbers/secrets · ALL-CAPS = urgent.

## PHASE 0 — in THIS order, before any work (v7 rule: connections first)

0. **CONNECTIONS FIRST (S208 owner directive).** Check and ask BEFORE any work:
   - folders connected? Need **`D:\dr-manoj-git`**, **`D:\Downloads`**, **`F:\ClinicBackup`**.
     Say which is missing and ask for it BY NAME. (`~/mnt/` shows what mounted; F: never
     mounts — reach it by the file-transfer tools only.)
   - **the assistant's browser**: open `https://followup.dr-manoj.in` in the browser pane. It
     was signed in as manoj at S208 and the profile persists — if it shows the sign-in page
     instead, ask the owner to sign in once in the pane before proceeding. The browser is how
     live pages are read and how publishes are verified.
1. Verify canon: `cd deploy_kits\KB_canon_all` → Windows: `VERIFY_KB_CANON.bat`; Linux shell:
   `md5sum -c MD5SUMS_ALL.txt`. **`RESULT: PASS` is the expectation, not any row count.**
2. Read Tier 0 only: `CANONICAL_MANIFEST.md` status · `KB_Register_v5_59_S208` ·
   `HANDOFF_RUNBOOK_2026-08-30_Session208close_v142` · `OWNER_TODO_LIVE.md` · any open incident.
   Open `D:\Downloads\ClaudeCowork\00_INDEX.md` for the extension map.
3. Feed check: `MARG_PICTURE.txt` (days with NO export should be 0) · `bank_match.log` on the
   VPS shows the 09:45 matcher runs (first scheduled morning: 30-Aug).
4. Then confirm, and ask which backlog item to start (Runbook §2 / OWNER_TODO_LIVE).

## WHAT IS LIVE SINCE S208 (the seven-kit night — details in Runbook v142)

Stock ledger (`/finance/stock`, computed-expected feed) · bank matcher (1,007 txns backfilled;
cron 09:45→12:00) · Darpan's day card + corrections (`/finance/darpan[...]`; guard OFF by
switch) · ledger tools + `/finance/pipeline` · staff register (`/finance/staff`; code register
seeded, next 101) · review tiles clickable · **the console** (coverage verdicts computed fresh ·
CN = sales-return back-audit vs the patient's own bill · untraceable return ⇒ owner approval ·
short-ID lookup).

## THE FIRST CANDIDATES FOR THIS SESSION

1. **Fold in the finance_app.py live md5** if the owner has pasted it (the one pending pin).
2. **Sprint 5**: patient master → VPS (GAS push from the Followup Tracker, VPS_Push_UPI
   pattern) · portal tile rename → "Marg sales pipeline" + landing page (one `cat
   /root/portal/<file>` from the owner, or read it via the signed-in browser).
3. **The 27-Aug transfer repair** the moment the amount surfaces (block in sprint plan v10).
4. Whatever the morning matcher runs surfaced on `/finance/pipeline`.

**Next free: D353 · F-218 (fork unratified; F-237–239 candidates) · A-D25 · Session 209.**
**Publish rule (S208): the ASSISTANT executes `PUBLISH_ALL_.bat` via computer control and
verifies success from the bat output — the owner only approves the access prompt.**

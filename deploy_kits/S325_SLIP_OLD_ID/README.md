# S325_SLIP_OLD_ID

The owner, first live morning: "2681 CLINIC ID FLAGGED AS NAYA". The patient master has no row for some old IDs. NEW now means above the highest known ID (his own rule); an old ID the master lacks reads *Purana mareez* and takes its name from Docterz. Today's wrongly marked slips are corrected by `fix_s325.py`. Walk: 81 checks.

```
cd /root/deploy/repo && git pull --ff-only && bash /root/deploy/repo/deploy_kits/S325_SLIP_OLD_ID/install_S325_SLIP_OLD_ID.sh
```

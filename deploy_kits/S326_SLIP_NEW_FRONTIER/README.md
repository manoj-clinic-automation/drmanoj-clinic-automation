# S326_SLIP_NEW_FRONTIER

The owner, after S325: a genuine new patient read *Purana mareez*. The ceiling now comes from IDs actually issued before the day, not from a stray master row ahead of the series. `fix_s326.py` re-decides every slip already written. Walk: 84 checks.

```
cd /root/deploy/repo && git pull --ff-only && bash /root/deploy/repo/deploy_kits/S326_SLIP_NEW_FRONTIER/install_S326_SLIP_NEW_FRONTIER.sh
```

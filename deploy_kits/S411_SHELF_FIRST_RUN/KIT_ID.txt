kit id S411_SHELF_FIRST_RUN
project PARENT (clinic), follow-up to S408 (session 283, 26-Sep-2026) -- the statement shelf's first real run read on the box: all 21 bank files were ICICI (12 iCRM monthly PDFs of six accounts, 4 pipe-delimited .txt, 5 password-locked Yes Bank PDFs), the identifier had read narrations instead of the header and the ICICI reader had never seen the real layout; the identifier now reads the header (bank, holder line, tail, period), holders match as word sets with the longest winning, three ICICI slots added, tails learned from the statement, .txt read, locked PDFs named, partial months never the month's statement; finance_icici v1.1 reads the iCRM PDF and the pipe .txt with the proof (B/F + rows = every running balance = the printed closing) into ICICI's own tables; the owner edits a slot's words; packs.py + packs.html patched from their live bytes, finance_icici.py replaced; the seed re-identifies the real files and prints the grid; restarts clinic-finance

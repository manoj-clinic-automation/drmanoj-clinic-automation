cd /root/portal && [ "$(md5sum tile_grants.json | cut -c1-32)" = "0efad736e71de7199e7c596a5b3d0c2e" ] && \cp -p tile_grants.json tile_grants.json.bak_S250_v14 && /root/wa/venv/bin/python3 -c "import json,os;f=\"tile_grants.json\";g=json.load(open(f,encoding=\"utf-8\"));U=g[\"users\"];T=\"Staff Register\";[U[n][\"mask\"].remove(T) for n in (\"shavez\",\"shivani\",\"alisha\") if T in U[n][\"mask\"]];g[\"version\"]=15;g[\"_note\"]+=\"  S250 v15 -- THE STAFF REGISTER REACHES THE PEOPLE WHO FILL IT, the owner's ruling of 13-Sep-2026: Shivani and Alisha are the register's makers and Shavez its checker, and none of the three could see it on their phone -- 'Staff Register' was masked from every staff login at S239 v9. The mask line is lifted for shavez, shivani and alisha ONLY; the tile carries roles ['doctor','manager','staff'] in portal.py, so that is the whole change -- nothing granted by name, no code edited, no restart. 'Attendance' stays masked for all three and the S239 v9 hold stands for every other staff login. The page's own gate is untouched: staff_register.py resolves maker/checker from SR_MAKER_USERS / SR_CHECKER_USERS, and D272 still bars a checker from approving a date he entered.\";json.dump(g,open(f+\".tmp\",\"w\",encoding=\"utf-8\"),indent=2,ensure_ascii=False);os.replace(f+\".tmp\",f)" && md5sum tile_grants.json; grep -rhE "^SR_(MAKER|CHECKER)_USERS" /root/staff_register/*.py 2>/dev/null | head -4

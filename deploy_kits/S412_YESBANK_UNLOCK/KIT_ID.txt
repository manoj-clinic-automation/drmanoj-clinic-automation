kit id S412_YESBANK_UNLOCK
project PARENT (clinic), follow-up to S408/S411 (session 283, 26-Sep-2026) -- the statement shelf opens Yes Bank's password-locked e-statements itself: the owner types the bank's statement password once on the Month-end packs page (stmt_secret, in finance.db only, never printed or echoed); the venv python (pypdf 4.3.1, installed there and stated) opens every locked PDF the candidates fit into <id>.unlocked.pdf beside the original, the file is then identified and read; 'locked -- no password' when nothing opens it; the branch's own statements stay the first source and a locked twin of a month already on the shelf is 'duplicate of branch copy'; the four non-Sanjeevni Yes Bank accounts go to yesbank_account_statement_* (never the shared tables the Bank card reads); the ICICI anchor moves only on a real period with a printed closing and is restored to 31-Aug; the pipe .txt's degenerate period is widened to its rows; packs.py, packs.html, stmt_shelf.py, finance_icici.py, finance_yesbank.py patched from their live bytes; restarts clinic-finance

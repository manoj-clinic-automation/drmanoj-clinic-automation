# S327_SLIP_ROOM_BOOK

The owner, first live day: room heading "X-ray room work"; the slip amount read-only on each room line before UPI / Cash / Done; the book section is "Start new book". `fix_s327.py` voids the pre-tile numbers (X-ray/Proc up to 1136). Every skipped number keeps its reason question (owner, restated). Walk: 87 checks.

```
cd /root/deploy/repo && git pull --ff-only && bash /root/deploy/repo/deploy_kits/S327_SLIP_ROOM_BOOK/install_S327_SLIP_ROOM_BOOK.sh
```

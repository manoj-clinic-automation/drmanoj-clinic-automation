S221_CARDS_SCOPED -- install order. ONE LINE AT A TIME. Use \cp to bypass the alias.

  0  copy the kit files into /root/finance/  (the one-paste block is in the chat)
  1  /root/wa/venv/bin/python3 -B /root/finance/patch_card_myjobs_s221.py
  2  \cp /root/deploy/repo/deploy_kits/S221_CARDS_SCOPED/cards_registry.json /root/finance/
  3  /root/wa/venv/bin/python3 -B /root/finance/walk_jaankari_s221_v2.py   (expect: WALK GREEN 31/31)
  4  systemctl restart clinic-finance.service
  5  md5sum /root/finance/darpan_card.html /root/finance/cards_registry.json

Step 3 is the CORRECTED walk for the kit installed earlier this morning; it runs on a COPY
of finance.db and proves that kit is sound on your real data. Step 5 must print the two
PREDICTED PINS. If either differs, say so; nothing else.

RENDER_TEST_myjobs_s221.py does NOT run on the box (no browser). It ran offline against
these exact bytes; EVIDENCE_render_myjobs.txt is its output.

The patcher refuses and changes nothing unless its anchor matches exactly once, keeps a
timestamped .bak, and refuses if the script tags come out unbalanced. Rolling back = restore
the .bak and put back the previous cards_registry.json.

WHAT CHANGES ON DARPAN'S PAGE: the seven-card directory becomes a row of buttons carrying
only HIS work -- Din ka card, Vaapsi desk, Stock count, Staff register. Amir's corrections desk
and the stock-differences screen leave his page. THIS IS A DISPLAY SCOPE, NOT A PERMISSION CHANGE: every page
keeps its own require() and unit_role rows, and reception's access to the Vaapsi desk is
untouched.

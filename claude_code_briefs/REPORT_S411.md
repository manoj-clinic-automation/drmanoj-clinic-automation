# REPORT S411_SHELF_FIRST_RUN — installed 26-Sep-2026 12:44–12:51 IST, working

## For Dr Manoj (plain English)
- **16 of the 21 bank statements are now read and on the shelf, none refused.** All 21 turned out to be ICICI files — and ICICI has **six** accounts, not three: Sanjeevni Medicos, Dr Manoj Agarwal Clinic and NK Pathology (current), Dr Manoj Kumar Agarwal, Dr Bhawna Agarwal and Manoj Kumar Agarwal HUF (savings). The shelf now has a cell for each. July and August are complete for all six: every statement read line by line and proven to the paisa (opening + every row = the closing the bank printed). Four of the 16 are ICICI's short text statements for odd dates; they are read too but never counted as a month's statement.
- **5 files still need you — not the drop-down, a copy:** they are Yes Bank PDFs that open only with a password (3 monthly savings statements, 2 "YFB monthly" current-account ones). The server does not have the password and will not guess it. Open each on your phone or PC with the bank's password, save it as a plain PDF, and put it in a folder named **Decrypted** inside *Bank Statements* on Drive — the shelf reads that folder every morning at 05:40 and will file them by the name printed inside. On your page they show as "locked — password needed" with the file name, so you can see which is which.
- **Your page** (`https://followup.dr-manoj.in/finance/packs`): the August grid shows the six ICICI cells read (Sanjeevni "matched") and the six Yes Bank cells empty until the copies arrive; the electricity line for August already reads the two auto-paid bills from the ICICI statements. If a statement ever lands in the wrong place, tap **words** on that cell and type the words the bank prints in the holder line — the shelf re-files on the spot.
- **What the September pack holds tonight:** the income sheet, the UPI totals and corrections, the payment digest and the scanned-bill bundles as before; **no bank statement yet** — September's monthly statements come from the banks in early October (two ICICI accounts show "partial" because a mid-month text statement is on the shelf; the month's own statement replaces it when it comes). August's pack, if you send it, carries the six ICICI statements. Amir's August pack still waits for the Yes Bank Sanjeevni statement (one of the locked five).
- Tested first on copies with crafted statements in ICICI's real shapes, then the real 21 in a scratch copy; nothing personal is in the report or the code. Every earlier build re-tested green.

## For the chat (S283)
**Kit** `deploy_kits/S411_SHELF_FIRST_RUN` (PARENT, follow-up to S408). Ran on the box from `/tmp/s411kit/S411_SHELF_FIRST_RUN` (byte-identical to the repository kit, `md5sum -c SUMS.md5` before each run); lock `/root/deploy/.claude_code_build.lock` owner S411_SHELF_FIRST_RUN held 12:35:47 → released after publish. Pins re-read live at 12:14 IST: packs.py 734c7fc0, stmt_shelf.py e74c29c0, finance_icici.py a79dce49, packs.html 03fb47f6, finance_yesbank.py 825016c0 — all as the brief expected.

**The diagnosis (read-only, 12:14–12:20 IST):**
- `stmt_file`: 21 bank-folder files. **8 refused** — 6 iCRM ICICI PDFs placed by words on `icici_sanj` / `icici_clinic` and refused by the S408 ICICI reader "no opening balance printed" (its layout assumed `Opening Balance … Closing Balance` text; ICICI prints a `B/F` row, a Summary row `Savings XXXX1234 <amount> Cr`, EN-dash dates, `Cr|Dr` suffixes, `Page Total:` rows over 2–3 pages); 2 iCRM ICICI PDFs (Dr Bhawna's savings) labelled **bank YES** by the identifier because a `UPI/…/YES BANK L` narration sits in the text, placed on `yes_cur_nk` / `yes_sav_bhawna` (the "NK PATHOLOGY" of a transfer narration), and refused by the Yes Bank reader ("does not print 'Period:'"). **14 unplaced** — 4 pipe-delimited ICICI `.txt` ("not a readable PDF": the identifier opened `.pdf` only), 5 Yes Bank PDFs that pdftotext refuses (`Incorrect password`; 3 `Statement_2026MTH08_*.pdf`, 2 `CS_*_YFB_monthly.pdf`), 4 iCRM PDFs whose holders no slot named (NK Pathology at ICICI ×2; Dr Manoj Kumar Agarwal at ICICI ×2 — the slot words "MANOJ AGARWAL" were matched as a phrase against the whole text and "MANOJ KUMAR AGARWAL" does not contain it), and All_Transactions.xlsx (fine).
- The live shelf held 98 files before this run (cards placed as locked originals / decrypted twins); `bank_statement_period` held only the one Yes Bank row (the S360 upload).

**Live files FROM → TO (md5 read back on the box after placing, 12:51:02 IST):**
| file | FROM (re-read live) | TO (read back) |
|---|---|---|
| /root/finance/packs.py | 734c7fc0f4ff780a94448e2b834f72b1 | afd429bf8c0e7f685adda7d688045ab7 |
| /root/finance/packs.html | 03fb47f68ffd28b375b50c91567cfcaa | b46d817c7a77944c4280ac72f95a6808 |
| /root/finance/finance_icici.py (v1.0 → v1.1, full-file revision of S408's file) | a79dce49bfd757a6db22aa68783d67a7 | 6d55a28aac170b1ab5899faa60eaa257 |

Not touched: `stmt_shelf.py` (e74c29c0 — the fetch already takes `.txt`), **`finance_yesbank.py` (825016c0 — no Yes Bank text ever reached it; all five Yes Bank files are locked)**, `sanjeevni_approvals.py`, the cron (05:40 left as it is). **Backups:** `finance.db.bak_S411_20260926_124432` (backup API, 25,329,664 bytes), `packs.py.bak_S411_734c7fc0`, `packs.html.bak_S411_03fb47f6`, `finance_icici.py.bak_S411_a79dce49`. **Restarted:** clinic-finance only (active; journal shows only the restart's own SIGTERM lines). **Health after:** `/finance/healthz` 200 · `/finance/packs` 302 (login gate, expected).

**What changed:**
1. **finance_icici v1.1** — three layouts: the iCRM PDF (holder line, Summary closing, `B/F` opening, EN-dash rows, `Cr|Dr`, autosweep / reverse-sweep columns, `Page Total:` skipped), the pipe `.txt` (columns taken from its header row; no closing printed → the running balance stands and `closing_printed=0` is recorded), the S408 generic text. **The proof is unchanged in kind and never loosened:** B/F + every row = every running balance = the printed closing; a row that does not carry the balance refuses the whole file naming the row; a Summary closing that differs from the last running balance refuses it. A locked PDF raises "password-protected PDF". **Lines go to `icici_statement_period` / `icici_statement_line`** (ICICI's own tables, same shape), NOT `bank_statement_*`: `sanjeevni_approvals.yesbank_position`, `bank_view`, `_statement_covers`, `_seen_in_yesbank` and S407's `bank_check` read `bank_statement_*` as the Yes Bank statement with no account filter — twelve ICICI statements there would have shown an ICICI closing as the Yes Bank balance and matched Yes Bank movements against ICICI lines.
2. **packs.py** — `identify_text` reads the **header** (the non-empty lines before the first dated / `B/F` / pipe row): the bank named FIRST, the holder line after "Your Details With Us:", the account tail (a new pattern for the pipe rows), the period. `place()` matches holders as word sets (`M/S.`, `DR.` dropped; SANJEEVANI folded), **a phrase printed as such ranks above a word set, then the longest match, then the kind; a tie is unplaced**. Three ICICI slots (`icici_nk`, `icici_bhawna`, `icici_huf`); the accounts' words as the banks print them (`S408_WORDS` names the old values so the seed replaces only those). **A by-words placement of a bank statement learns the tail**; `process_inbox` re-passes while a pass learns tails (the two September `.txt`, fetched before their account's PDF, landed in the second pass). `.txt`/`.csv` read; a locked PDF gets `LOCKED_NOTE`; the owner's tap on a locked file records "locked original". `cells()`: a file covering only part of the month is **`partial`**, never the month's statement (pack row "missing — only dd-Mon → dd-Mon on the shelf"; Amir's pack ignores partials). `electricity_lines` reads `icici_statement_line`. New route `POST /finance/packs/api/slot-words` (owner). `unplaced()` carries `holder` and `locked`.
3. **packs.html** — the unplaced table shows the holder text or "locked — password needed"; a **words** tap on each cell.
4. **seed_s411.py** — 3 slots added (15 now), 6 word sets replaced (`yes_cur_sanj`, `yes_sav_manoj`, `yes_sav_bhawna`, `yes_sav_huf`, `icici_sanj`, `icici_personal`), every bank file's identification cleared and re-run.

**Live result (the seed's own print, 12:50):** 21 bank files re-identified → placed 16, read 16, refused 0, unplaced 6 (the 5 locked + All_Transactions.xlsx); by words 6 (the first statement of each ICICI account), by tail 10 (the second month and the four `.txt`); tails learned 6; `icici_statement_period` 16 rows (6 accounts; 12 icrm + 4 pipe), `icici_statement_line` 292 rows; `bank_statement_period` still 1 row / 7 lines (Yes Bank, untouched). The 05:40 fetcher run by hand at 12:51: `fetch: 0 new, 99 seen · identify: unplaced 6, refused 0`.

**The month grid (states only):**
| cell | Jul-2026 | Aug-2026 | Sep-2026 |
|---|---|---|---|
| Yes Bank: NK Pathology · Clinic · Sanjeevni (current); Dr Manoj · Dr Bhawna · HUF (savings) | empty ×6 | empty ×6 | empty ×6 |
| ICICI Sanjeevni Medicos | matched | matched | **partial** (text statement 10-Aug…10-Sep) |
| ICICI Dr Manoj Agarwal Clinic | read | read | **partial** (text statement 15-Aug…14-Sep) |
| ICICI NK Pathology · Dr Manoj · Dr Bhawna · HUF | read ×4 | read ×4 | empty ×4 |

**Walk (run 5):** `WALK_S411 GREEN -- 13/13`. (1) crafted statements in ICICI's real shapes: the HUF file with a Yes Bank narration and "SANJEEVNI" inside lands on the ICICI HUF slot; Dr Bhawna's with "NK PATHOLOGY" in a narration on ICICI Bhawna; Dr Manoj Kumar Agarwal on ICICI personal (not HUF, not clinic); clinic / NK / Sanjeevni right; a look-alike (RAJESH AGARWAL) unplaced with its holder text; a Yes Bank head with no "Your Details" block placed by its own words; every by-words placement learns the tail and the pipe `.txt` lands by tail; the six iCRM files + the July one + the pipe file read; **the tampered NK July file REFUSED: "row 03-07-2026 'EBA/eATM 03AUG/…': 3,15,938.70 does not carry the balance from … to …"**; the lines in `icici_statement_*` (25), nothing in `bank_statement_*`; the locked PDF named, the owner's tap files it as a locked original; darpan cannot edit words (302), the owner can and the look-alike lands on the edited slot; the grid (August six ICICI read/matched, July NK "arrived" — its file is the tampered one, September Sanjeevni partial); the pack rows (August ICICI ready, the electricity row with the two ICICI lines, September's Sanjeevni "missing — only 15-Aug-2026 → 14-Sep-2026 on the shelf", Amir's pack ready for August, not September). (2) **the real 21 files** in a scratch copy: the reader over each — read 16 (12 icrm with the printed closing proven, 4 pipe), locked 5, refused 0, lines per file 9…35; after the seed — 16 placed and read (6 by words, 10 by tail), 0 refused, 0 left over, the 5 locked named, the six ICICI cells of July and August read/matched, 6 tails learned, `bank_statement_period` unchanged. (3) negative controls on the S408 code with the same crafted files: bank flipped to YES, the HUF and Bhawna files on Yes Bank slots, "no opening balance printed", `.txt` unreadable, no tail learned.

**Earlier walks re-run on the patched files:** S410 32/32 · S409 24/24 · **S408 26/27 + the declared supersession** · S407 27/27 · S406 27/27 · S405 29/29 · S404 65/65 · S403 52/52 · S400 63/63 · S402 16/16. **The supersession, stated plainly:** S408's frozen walk has one check "the ICICI reader read the fixture into bank_statement_period/_line …". S411 deliberately moves ICICI lines out of those tables (reason in "What changed" 1). The installer accepts S408's walk only when its single red is that named check and the other 26 are green; the acceptance line is printed in the install log. The earlier S403/S405/S407/S408 scratch-copy resets from S410's installer are carried over.

**Runs it took (each red placed nothing):** run 1 — the old-app control indexed a field S408's rows do not have (probe fix). Run 2 — three real findings from the crafted files: the header cut still swallowed the first transaction rows (a narration could name the other bank) → the header now ends at the first dated / `B/F` / pipe row and the bank is the one named FIRST; the pipe `.txt` account tail was not matched (a pattern for its rows); the fixture PDFs had lost their EN dashes (encoding) — walk fix. Run 3 — the two September `.txt` files were fetched before the PDFs that teach their tail → `process_inbox` now re-passes while tails are being learned. Run 4 — S408's frozen seed writes the OLD words, under which a word-set matcher let "MANOJ AGARWAL" beat "HUF" → the phrase-first ranking. **Run 5 — green, placed 12:50.**

**Found, for the chat (F):**
1. **The owner's account list under-counted ICICI** (three of six). The three new slots are labelled "(ICICI current/savings)"; the owner can edit the words on the page.
2. **All five Yes Bank statements are password-locked PDFs.** The shelf cannot read them; the fix is a decrypted copy in *Bank Statements/Decrypted* (the fetcher already walks subfolders of *Bank Statements*, one level). No Yes Bank text has been seen yet, so the Yes Bank reader is unproven on these two Yes Bank PDF layouts — the first decrypted copy decides; a refusal will be visible in the cell.
3. **`bank_anchor` (medical, icici) moved from 31-Aug to 10-Sep** by S408's `_anchor_refresh` when the Sanjeevni pipe `.txt` (rows to 10-Sep, exported 11-Sep) was read: its last running balance, proven from B/F, is the bank's own end-of-day figure, so `icici_position` now starts from 10-Sep and adds settlements dated on/after it. Worth a glance on the Bank card; the previous anchor is in `finance.db.bak_S411_20260926_124432`.
4. ICICI's pipe `.txt` prints a degenerate header period for some files ("from 10-09-2026 to 10-09-2026" over rows from 11-Aug): the shelf and the ICICI period row keep the header's dates; the lines carry the true dates. A later revision can widen the period to the rows.
5. The `slot-words` edit re-identifies only unplaced files; a file already placed wrongly needs the owner's drop-down.

**Not done / outside the brief:** `finance_yesbank.py` untouched (declared above); no cron change; no Yes Bank password anywhere; nothing in /root/gutlog, /root/rxguard, /root/fitlog, /srv/family, /root/family. No permission prompt was hit.

**Undo:** the three `.bak_S411_<from8>` files back, `systemctl restart clinic-finance`, healthz 200. The re-identification, the three slots, the learned tails and the ICICI tables are data and harmless; the database backup is used only if the owner says so.

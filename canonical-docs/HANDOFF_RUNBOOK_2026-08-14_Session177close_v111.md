# HANDOFF RUNBOOK — v111 (2026-08-14 · Session 177 close)

*Tier 0. §0 what happened last · §2 live backlog (⭐ top task at head) · §3 install discipline. Companion to the KB Register v4.6 (state) + Archive v1.25 (history). This session shipped the Asset Register A-D24 wave LIVE (v1.11.0), closed the housekeeping punch (backup pruner · shared-sarvam verify · test-row delete), and executed the owed S173–S176 clinic-canon fold at EOS.*

## §0 — WHAT HAPPENED LAST (S177)

- **A-D24 wave BUILT + INSTALLED LIVE (asset app v1.10.3 → v1.11.0; VPS smoke 342/0).** (a) `/intake` now leads with the full shadow-flatten **scanner widget** (doc-mode locked; `<details>` plain-upload fallback; new routes `/intake/scan_submit` + `/intake/slip/last`; `RECEPTION_OK` deliberately widened by exactly those three endpoints after the fail-closed guard correctly 403'd them). (b) `/purchases` **spend analytics** — total/this-month cards, spend-by-month + top-vendor bars, Indian-format `_inr()` rupees, owner dashboard spend tile. (d) **approved-bill → asset supplier/purchase-date backfill** — non-clobber, idempotent, hooked into init_db + bill_approve + the edit-bridge. (c) was already satisfied (A-D16 shared sarvam import). Install = one &&-chained block: md5 gate → `.bak` → `mv` → VPS smoke gate → restart (auto-rollback on red). `.bak` files retained in `/root/assetapp/`.
- **Housekeeping (item 4) ALL DONE.** `prune_backups.py 9dce8ea6…` at `/root/` (self-test 15/0; dry-run default, keep-newest-2, 14-day gate, live-file guard; manual — no cron; first dry-run rightly pruned nothing). Shared `sarvam_ocr.py` verified already single-copy at `/root/shared/` (pin matched — the KB's ASSUMED flag closed, nothing moved). **Test assets #51–55 CLEAN-DELETED** (54 → 49, zero orphans) via guarded `delete_test_assets.py`; undo backup `assets.db.predelete.2026-08-14_223751` on the VPS (delete it when satisfied — the pruner ignores that suffix). Rows **#45–50** (dup "Gaurav scientific"×3 / "Aastha medical"×4, manager practice entries, 3 with attachments) flagged, NOT touched.
- **Canon fold executed at this EOS (the S173–S176 debt).** §S173–§S176 appended to the Archive (prefix-hash proven, +17,723 chars) from the md5-verified close-out records; Register → v4.6 (A-D1–A-D24 indexed; F-83); Estate Inventory → v1.1 (Asset-Register row v1.11.0 + `/root/shared/` row); `KB_Asset_Register_v1_10_3.md` `07d01e80…` registered Tier-1 (retires v1.8.1); consolidated **F-82 + F-83** Fault-Register append shipped (→ v2.17 on owner apply); manifest advanced **S172 → S177**.
- **Delivery rules minted (standing):** files pre-named `.new` + ONE copy-paste install block; never numbered steps; never pasted heredocs (they corrupt at the owner's terminal — utility scripts ship as uploads).
- **No clinic decision (D313 free) · no clinic finding (F-84 free) · asset A-D24 minted (next A-D25) · no incident.** Full narrative: Archive §S177.

**Live now (asset app):** `asset_register.py 0cd8fc3bfe8d39322c6162a41124bddf` (v1.11.0) · `smoke_test.py 6e72373325f808b1d7eaeb99f51a7b14` (342) · `scanner_widget.js 4fe8c893…` · `/root/shared/sarvam_ocr.py b1cc567b…` · `assets.db` 49 assets. **Live (clinic, UNCHANGED from S172/S176):** `portal.py da4177091ba9f188be6a0ff3eaf25bd8` · `casepack_portal.py 341404d7` · `portal_wa.py 34994b23` · `portal_followups.py 98547bc4` · `portal_console.py 552135b5` (cron `*/10 9-21`) · `daily_digest.py 8140f543` · `portal_gist.py 55e111d7` · `salary_engine.py 5514918` · `staff_register.py cef76859` · `staff_ledger.py 92665b64` · `att_month_report.py e64cad19`. **PHI stores (F-31/F-49, ALL gitignored):** `console.db` · `transcript_cache.db` · `console_reviews.db` · `rec_cache/` · `/root/wa/casepack/` · `/root/wa/wa_portal/wa_portal_sends.csv` · `/root/wa/followups/` · asset `assets.db` + `uploads/`.

## §2 — LIVE BACKLOG

⭐ **S178 top task — WABA GO-LIVE (F-82).** Still BLOCKED vendor-side (MyOperator HTTP 500 on all authenticated calls). Check the vendor ticket (Khushi/Lokesh); fast probe = `GET /chat/templates` → 200 not 500. THEN: `PORTAL_WA_DRYRUN → "0"` in `/root/portal/portal_config.py` → restart `clinic-portal.service` → self-send `drmanoj_post_visit` to the doctor's own number → confirm → live. No code change. **Coordinate with Lokesh before ANY WABA token rotation.**

**Then (owner-priority order to confirm):**
1. **Owner action — commit the S177 git kit** (`gitkit_S177.zip`): `assetapp/asset_register.py` (v1.11.0) + `smoke_test.py` + `inspect_assets.py` + `delete_test_assets.py` + `tools/prune_backups.py` + the canonical-docs mirror of this EOS set. Repo is at v1.10.3 (S176 kit committed); this lifts assetapp to v1.11.0. Behind it: the S162–S172 clinic-side mirror backlog (portal/console/casepack files — `.gitignore` the PHI paths FIRST).
2. **Security (owner action):** rotate the Asset Register `/api/due` WhatsApp token (/admin → Rotate) + update its cron; also the portal `/api/due` token seen in earlier screenshots.
3. **Asset rows #45–50 review** (dup vendor practice entries) — keep or delete; `delete_test_assets.py` pattern reusable (new IDs + names, re-gate offline first).
4. **Asset-app next (A-D25 candidates):** durable OCR worker (F-83's fire-and-forget thread → queue or synchronous-with-timeout) · consumption dashboards on the bills ledger · Phase-E Sarvam schema tuning on more real bills · scanner-app adoption of `/root/shared/sarvam_ocr.py`.
5. **Three S176-ideated clinic features (not yet built; 4 open decisions before build):** reception-tier scan-submit intake beyond bills? (scope) · approval split rule/threshold · doctor-only staff cockpit "fronts existing writers" framing · salary-touching parts wait for the August APPROVE cycle (~Sept 9)?
6. Console follow-ons: w8/w9 (universal row redesign · sticky headers · day grouping · no-show banner · hot leads · staff week matrix · `/portal/console/staffreport`) · D223 gist tile build (feeds live) · nightly `console_reviews.db` → Drive backup (candidate D306 tail) · F-81 duplicate call rows · MyOperator OBD click-to-call · outcome-options admin UI.
7. **Carried:** service-account key rotation (overdue, Tier A — coordinate Lokesh) · `wa_approve` nohup→systemd verify-first · unaccounted `ANTHROPIC_API_KEY` in `/root/wa/.env` · F-69 dead `Call_Feed` writer · F-70 Callback Tracker Core Dossier update · F-71 rotation check · August salary reconciliation at month-end (first real APPROVE ~Sep 01–09) · Track V revenue (needs the PC) · Item 10 rubric (owner red-pen) · Docterz export migration (parked).

## §3 — INSTALL DISCIPLINE
Files delivered **pre-named `.new`**; install = **ONE &&-chained block**: `md5sum -c` the `.new` files → `cp live live.bak` → `mv .new → live` → **run the smoke test as a gate** → restart the service ONLY on green → auto-restore from `.bak` on red. Never numbered steps; never pasted heredocs (owner terminal corrupts long pastes — ship scripts as uploads). A filename is not provenance — trust the hash (F-66). **Asset app runs on the system `python3`** (gunicorn `#!/usr/bin/python3`), NOT the `/root/wa` venv (F-53); clinic `/root/wa` scripts use `/root/wa/venv/bin/python3`. Portal lives at `/root/portal/`. Flask route change → F-63 test-client hits on ACTUAL routes; served-HTML assertions incl. absence checks (F-79). New/altered table → `--init`/CREATE-IF-NOT-EXISTS before the page queries it (F-65). One writer per store (D235). Never slice a date string — parse it (F-78). WhatsApp go-live = DRY→live + self-send-to-own-number FIRST (D310). Salary/PHI never in repo/kit (F-31/F-49). DB mutations ship as guarded scripts (refuse-all identity check + WAL-checkpointed backup + all-or-nothing txn), never ad-hoc SQL.

**END OF HANDOFF RUNBOOK v111 (Session 177).**

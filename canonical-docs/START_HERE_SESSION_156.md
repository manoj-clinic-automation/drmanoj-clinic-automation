# START HERE — SESSION 156 (regenerated at S155 close)

Paste the evergreen START_HERE_PROMPT_v5 opener, or just type **start** — then Phase 0 runs (D247):
verify **every** row of `CANONICAL_MANIFEST.md` by md5 (all tiers); read into context ONLY Tier 0
(this file · KB_Register v2.8 · HANDOFF_RUNBOOK v93 · any open incident — none open).

**State in one breath:** D258 EXECUTED — the Staff Ledger (v2.4, selftest 123, live at
`attendance.dr-manoj.in/ledger`) is the single home for ALL staff money including Darpan's
structured loan (migrated + verified to the rupee; July close done: loan 179,000, July line −5,000);
the workbook's loan sheets are RETIRED and the workbook's canonical home is now VPS
`/root/clinic_salary/`. F-49 closed by ruling; F-50 minted+fixed (explicit role allow-lists);
Phase-2 repo trim pushed; folder digests re-pinned with the recipe now documented.

**TOP JOB (owner mandate, S155): full backend salary automation** — kill the manual monthly
Excel entry/upload entirely; the VPS computes salaries end-to-end (attendance report × ledger
close × staff master). Start with a design pass: output/approval/payslip shape, then build.
Bundle backlog item 3 (watchdog coverage for `staff-ledger.service`) into the same code session.

Small owner action possibly still open: the Shivani test-fine contra (Runbook §2.2).

**Backlog:** HANDOFF_RUNBOOK v93 §2. **Next free: D259 · F-51.** Notion catch-up spans S151–S155.

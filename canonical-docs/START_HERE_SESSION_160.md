# START HERE — SESSION 160 (regenerated at S159 close)

Paste the evergreen START_HERE_PROMPT_v5 opener, or just type **start** — then Phase 0 runs (D247):

1. Open **`CANONICAL_MANIFEST.md`** (Tier 0, the linchpin).
2. **md5-verify every row, all tiers** — a mismatch halts work until reconciled (D172/D188). A filename is not provenance.
3. Read into context **Tier 0 only:** this file, the **KB Register** (v3.2), the **HANDOFF_RUNBOOK** (v97), and any open incident. Open Tier 1 only when the task touches it; Tier 2 is hash-verified but never read in the loop and never edited without a waiver (D34).
4. Confirm, then ask which backlog item to start (**HANDOFF_RUNBOOK §2** = the live backlog).

---

**Where S159 left it (one paragraph).** A build session on the doctor portal. **Item 3 (Portal SSO Group D) is DONE and verified live** — `portal.py` → **`679a00874c039ecabc533f9ddd0f5e67`** (service `clinic-portal`, :8099): 4 Clinic-PC-only tiles gated by a per-device marker (`/portal/mark-pc`), 2 personal tiles reading git-ignored `portal_config.py`, and **GMB Review Assist moved to the VPS** at `/portal/gmb` behind login (`gmb_serve.py` retired). Group C report-Sheet tiles were found already wired. **CC→Tally VPS-hosting was declined** (D262/D269 — local finance/PHI stays local). Decisions **D267–D269**, findings **F-59–F-61** (Chrome ERR_UNSAFE_PORT 5060/5061; VPS case-sensitivity; config code-fence paste).

**Top of the backlog (Runbook §2):** (1) commit `portal.py` `679a0087…` → `launcher/` + externally md5-verify the six S158 files landed (needs the **repo-owner path**); (3) 🆕 hands-on session to confirm the 3 local PC apps (5057/5058/5059) run + a reliable launcher — **not VPS**, and owner ruled Task Scheduler/NSSM out. **Also owed / urgent:** rotate the Google service-account key (F-56) + CALLHOOK Steps 3–4. July salary reconciliation is an owner carry; August salary run (~Sep 01–09) is the first real APPROVE.

**Backlog:** HANDOFF_RUNBOOK v97 §2. **Next free: D270 · F-62.**
